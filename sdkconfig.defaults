CONFIG_ESP32_PHY_MAX_WIFI_TX_POWER=20
CONFIG_ESP32_PHY_MAX_TX_POWER=20

CONFIG_FREERTOS_UNICORE=n
CONFIG_FREERTOS_NO_AFFINITY=0x7FFFFFFF
CONFIG_FREERTOS_CORETIMER_0=y
CONFIG_FREERTOS_OPTIMIZED_SCHEDULER=y
CONFIG_FREERTOS_HZ=1000
CONFIG_FREERTOS_SUPPORT_STATIC_ALLOCATION=y
CONFIG_FREERTOS_USE_STATS_FORMATTING_FUNCTIONS=y
CONFIG_FREERTOS_IDLE_TASK_STACKSIZE=2048
CONFIG_FREERTOS_ISR_STACKSIZE=2048
CONFIG_FREERTOS_ENABLE_BACKWARD_COMPATIBILITY=y

CONFIG_COMPILER_OPTIMIZATION_PERF=y
CONFIG_COMPILER_OPTIMIZATION_ASSERTIONS_ENABLE=y
CONFIG_COMPILER_STACK_CHECK_MODE_NONE=y

CONFIG_FREERTOS_TIMER_TASK_STACK_DEPTH=4096
CONFIG_ESPTOOLPY_FLASHMODE_QIO=y
CONFIG_ESPTOOLPY_FLASHFREQ_80M=y
CONFIG_ESPTOOLPY_FLASHSIZE_8MB=y
CONFIG_ESPTOOLPY_FLASHSIZE="8MB"

CONFIG_FREERTOS_VTASKLIST_INCLUDE_COREID=y
CONFIG_FREERTOS_USE_TRACE_FACILITY=y
CONFIG_FREERTOS_GENERATE_RUN_TIME_STATS=y

# ---- pyDrone (01Studio, ESP32-S3) pin mapping ----
# Motors (N-MOS gate drive): M1=GPIO4, M2=GPIO5, M3=GPIO40, M4=GPIO41
CONFIG_MOTOR01_PIN=4
CONFIG_MOTOR02_PIN=5
CONFIG_MOTOR03_PIN=40
CONFIG_MOTOR04_PIN=41
# Sensor I2C bus (MPU6050; SPL06 sits behind it on the aux bus)
CONFIG_I2C0_PIN_SDA=16
CONFIG_I2C0_PIN_SCL=15
# MPU6050 data-ready interrupt
CONFIG_MPU_PIN_INT=7
# Battery voltage ADC stays on GPIO2 (= ADC1 channel, default), divider handled in pm_esplane.c
# Status LEDs (active high): blue = system/link-down/calibrating, green = link up.
# pyDrone has no MCU-controlled red LED (red/yellow are HW power/charge), so park
# LED_PIN_RED on GPIO21 (P2 header only, no onboard peripheral) to avoid conflicts.
CONFIG_LED_PIN_BLUE=46
CONFIG_LED_PIN_GREEN=42
CONFIG_LED_PIN_RED=21
